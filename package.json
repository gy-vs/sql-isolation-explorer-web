{
  "name": "mvcc-isolation-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "SQL isolation level exploration workbench on an in-memory MVCC model",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/"
  }
}
